// import { Router } from "express";
// import { auth } from "../../../middleware/authMiddleware";
// import { destroy, index, show, store, update } from "./controller";

// const router = Router();

// router.get("/", auth, index);
// router.get("/:gl_number", auth, show);
// router.post("/", auth, store);
// router.patch("/:gl_number", auth, update);
// router.delete("/:gl_number", auth, destroy);

// export { router as accGlTransRoute };
