// import { Router } from "express";
// import { auth } from "../../../middleware/authMiddleware";
// import { destroy, index, pegawaiJadwal, show, store, update } from "./controller";

// const router = Router().use(auth);

// router.get("/", index);
// router.get("/pegawai-jadwal", pegawaiJadwal);
// router.get("/:id", show);
// router.post("/", store);
// router.patch("/:id", update);
// router.delete("/:id", destroy);

// export { router as jadwalKerjaRoute };

