// import { Router } from "express";
// import { index, show, store, update, destroy } from "./controller";
// import { auth } from "../../../middleware/authMiddleware";
// import { hariLiburTipeRoute } from "./tipe/route";

// const router = Router().use(auth);

// router.use("/tipe", hariLiburTipeRoute)

// router.get("/:year", index);
// router.get("/:name", show);
// router.post("/", store);
// router.patch("/:id", update);
// router.delete("/:id", destroy);

// export { router as hariLiburRoute };
